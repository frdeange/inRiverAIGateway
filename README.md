# inRiver AI Gateway

The demo now provisions Azure AI Foundry resources using ARM templates under `demo\arm-templates` (`Microsoft.CognitiveServices/accounts`, `accounts/projects`, `accounts/deployments`, API `2025-10-01-preview`).

Use `demo\provision-resources.ps1` to deploy Sweden Central, France Central, and North Europe regional Foundry stacks.
